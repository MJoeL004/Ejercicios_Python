from flask import Flask, jsonify, request
import psycopg2

app = Flask(__name__)

DB_CONFIG = {
    "host": "localhost",
    "database": "curso_flask",
    "user": "postgres",
    "password": "12345",
    "port": "5432",
    "client_encoding": "utf8"
}

def obtener_conexion():
    return psycopg2.connect(**DB_CONFIG)
@app.route('/', methods=['GET'])
def inicio():
    return jsonify({"mensaje": "Inicio", "entidades": ["/usuarios", "/productos", "/tareas", "/peliculas"]})

# ==========================================
# 1. ENTIDAD: USUARIOS
# ==========================================

@app.route('/usuarios', methods=['GET'])
def obtener_usuarios():
    conexion = obtener_conexion()
    cursor = conexion.cursor()
    cursor.execute("SELECT id, nombre FROM usuarios ORDER BY id")
    datos = cursor.fetchall()
    usuarios = [{"id": u[0], "nombre": u[1]} for u in datos]
    cursor.close()
    conexion.close()
    return jsonify(usuarios)

@app.route('/usuarios/<int:id>', methods=['GET'])
def obtener_usuario(id):
    conexion = obtener_conexion()
    cursor = conexion.cursor()
    cursor.execute("SELECT id, nombre FROM usuarios WHERE id=%s", (id,))
    usuario = cursor.fetchone()
    cursor.close()
    conexion.close()
    
    if usuario:
        return jsonify({"id": usuario[0], "nombre": usuario[1]})
    return jsonify({"error": "Usuario no encontrado"}), 404

@app.route('/usuarios', methods=['POST'])
def crear_usuario():
    datos = request.get_json()
    conexion = obtener_conexion()
    cursor = conexion.cursor()
    cursor.execute('INSERT INTO usuarios(nombre) VALUES(%s) RETURNING id', (datos['nombre'],))
    nuevo_id = cursor.fetchone()[0]
    conexion.commit()
    cursor.close()
    conexion.close()
    return jsonify({'id': nuevo_id, 'nombre': datos['nombre']}), 201

@app.route('/usuarios/<int:id>', methods=['PUT'])
def actualizar_usuario(id):
    datos = request.get_json()
    conexion = obtener_conexion()
    cursor = conexion.cursor()
    cursor.execute('UPDATE usuarios SET nombre=%s WHERE id=%s', (datos['nombre'], id))
    conexion.commit()
    cursor.close()
    conexion.close()
    return jsonify({'mensaje': 'Usuario actualizado correctamente'})

@app.route('/usuarios/<int:id>', methods=['DELETE'])
def eliminar_usuario(id):
    conexion = obtener_conexion()
    cursor = conexion.cursor()
    cursor.execute('DELETE FROM usuarios WHERE id=%s', (id,))
    conexion.commit()
    cursor.close()
    conexion.close()
    return jsonify({'mensaje': 'Usuario eliminado correctamente'})

# ==========================================
# 2. ENTIDAD: PRODUCTOS
# ==========================================

@app.route('/productos', methods=['GET'])
def obtener_productos():
    conexion = obtener_conexion()
    cursor = conexion.cursor()
    cursor.execute("SELECT id, nombre, precio FROM productos ORDER BY id")
    datos = cursor.fetchall()
    productos = [{"id": p[0], "nombre": p[1], "precio": float(p[2])} for p in datos]
    cursor.close()
    conexion.close()
    return jsonify(productos)

@app.route('/productos', methods=['POST'])
def crear_producto():
    datos = request.get_json()
    conexion = obtener_conexion()
    cursor = conexion.cursor()
    cursor.execute("INSERT INTO productos(nombre, precio) VALUES(%s, %s) RETURNING id", (datos['nombre'], datos['precio']))
    nuevo_id = cursor.fetchone()[0]
    conexion.commit()
    cursor.close()
    conexion.close()
    return jsonify({"id": nuevo_id, "nombre": datos['nombre'], "precio": datos['precio']}), 201

@app.route('/productos/<int:id>', methods=['GET'])
def obtener_producto(id):
    conexion = obtener_conexion()
    cursor = conexion.cursor()
    cursor.execute("SELECT id, nombre, precio FROM productos WHERE id=%s", (id,))
    producto = cursor.fetchone()
    cursor.close()
    conexion.close()
    
    if producto:
        return jsonify({"id": producto[0], "nombre": producto[1], "precio": float(producto[2])})
    return jsonify({"error": "Producto no encontrado"}), 404

@app.route('/productos/<int:id>', methods=['DELETE'])
def eliminar_producto(id):
    conexion = obtener_conexion()
    cursor = conexion.cursor()
    cursor.execute("DELETE FROM productos WHERE id=%s", (id,))
    conexion.commit()
    cursor.close()
    conexion.close()
    return jsonify({"mensaje": "Producto eliminado correctamente"})

# ==========================================
# 3. ENTIDAD: TAREAS
# ==========================================

@app.route('/tareas', methods=['GET'])
def obtener_tareas():
    conexion = obtener_conexion()
    cursor = conexion.cursor()
    cursor.execute("SELECT id, titulo, completada FROM tareas ORDER BY id")
    datos = cursor.fetchall()
    tareas = [{"id": t[0], "titulo": t[1], "completada": t[2]} for t in datos]
    cursor.close()
    conexion.close()
    return jsonify(tareas)

@app.route('/tareas', methods=['POST'])
def crear_tarea():
    datos = request.get_json()
    conexion = obtener_conexion()
    cursor = conexion.cursor()
    cursor.execute("INSERT INTO tareas(titulo) VALUES(%s) RETURNING id", (datos['titulo'],))
    nuevo_id = cursor.fetchone()[0]
    conexion.commit()
    cursor.close()
    conexion.close()
    return jsonify({"id": nuevo_id, "titulo": datos['titulo'], "completada": False}), 201

@app.route('/tareas/<int:id>', methods=['GET'])
def obtener_tarea(id):
    conexion = obtener_conexion()
    cursor = conexion.cursor()
    cursor.execute("SELECT id, titulo, completada FROM tareas WHERE id=%s", (id,))
    tarea = cursor.fetchone()
    cursor.close()
    conexion.close()
    
    if tarea:
        return jsonify({"id": tarea[0], "titulo": tarea[1], "completada": tarea[2]})
    return jsonify({"error": "Tarea no encontrada"}), 404

@app.route('/tareas/<int:id>', methods=['DELETE'])
def eliminar_tarea(id):
    conexion = obtener_conexion()
    cursor = conexion.cursor()
    cursor.execute("DELETE FROM tareas WHERE id=%s", (id,))
    conexion.commit()
    cursor.close()
    conexion.close()
    return jsonify({"mensaje": "Tarea eliminada correctamente"})

# ==========================================
# 4. ENTIDAD: PELÍCULAS
# ==========================================

@app.route('/peliculas', methods=['GET'])
def obtener_peliculas():
    conexion = obtener_conexion()
    cursor = conexion.cursor()
    cursor.execute("SELECT id, titulo, director FROM peliculas ORDER BY id")
    datos = cursor.fetchall()
    peliculas = [{"id": p[0], "titulo": p[1], "director": p[2]} for p in datos]
    cursor.close()
    conexion.close()
    return jsonify(peliculas)

@app.route('/peliculas', methods=['POST'])
def crear_pelicula():
    datos = request.get_json()
    conexion = obtener_conexion()
    cursor = conexion.cursor()
    cursor.execute("INSERT INTO peliculas(titulo, director) VALUES(%s, %s) RETURNING id", (datos['titulo'], datos['director']))
    nuevo_id = cursor.fetchone()[0]
    conexion.commit()
    cursor.close()
    conexion.close()
    return jsonify({"id": nuevo_id, "titulo": datos['titulo'], "director": datos['director']}), 201

@app.route('/peliculas/<int:id>', methods=['GET'])
def obtener_pelicula(id):
    conexion = obtener_conexion()
    cursor = conexion.cursor()
    cursor.execute("SELECT id, titulo, director FROM peliculas WHERE id=%s", (id,))
    pelicula = cursor.fetchone()
    cursor.close()
    conexion.close()
    
    if pelicula:
        return jsonify({"id": pelicula[0], "titulo": pelicula[1], "director": pelicula[2]})
    return jsonify({"error": "Película no encontrada"}), 404

@app.route('/peliculas/<int:id>', methods=['DELETE'])
def eliminar_pelicula(id):
    conexion = obtener_conexion()
    cursor = conexion.cursor()
    cursor.execute("DELETE FROM peliculas WHERE id=%s", (id,))
    conexion.commit()
    cursor.close()
    conexion.close()
    return jsonify({"mensaje": "Película eliminada correctamente"})

if __name__ == '__main__':
    app.run(debug=True)
